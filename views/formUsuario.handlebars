{{#if usuario }}
 <h1>Atualizar Cadastro:</h1>
 <form action="/usuario/{{usuario.id}}/atualizar" method="post">

{{else}}

 <h1>Cadastrar:</h1>
 <form action="/usuario/novo" method="post">
{{/if}}

        <div>
            <label for="nickname">Nickname</label>
            <input type ="text" name="nickname" id="nickname" value="{{usuario.nickname}}"/>
            </div>
            <div>
                <label for="nome">Nome</label>
                <input type="text" name="nome" id="nome" value="{{usuario.nome}}"/>
        </div>
     {{#if usuario}}
     <input type="submit" value="Salvar"/>
     {{else}}
     <input type="submit" value="Atualizar"/>
     {{/if}}
        <input type="submit" value="Enviar"/>
     </form>